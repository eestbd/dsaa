#include "Search.h"

void Search_MMST(Graph &graph, int K) {
  // ============ Fill this Part =========== //
  // Goal: Find K connected nodes (subgraph_size) to maximize the Score_MMST.
  // Score_MMST = sum(node_weights) + sum(MST_edge_weights)
  // Insert your selected nodes into the graph using graph.submit_node()

  // ========= End of Filling Part ======== //
}
